# 详细设计：视口（MVP）

## 职责

- 使用 `<canvas>` 全宽全高填充中央区域。
- 绘制浅灰背景 + 网格线（世界单位可简化为像素）。
- 若有 `lastResult`，在画布一角绘制文本摘要（骨架名、骨骼数）。

## 坐标系

- 原点在画布中心，Y 轴向上（与常见 2D 工具一致）；MVP 可仅用 2D 上下文 `translate` 到中心。

## 后续

- 骨骼线段、图片附件、权重绘制接 WebGL 或分层 Canvas。
