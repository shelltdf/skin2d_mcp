# skin2d-editor-web

## 产物

- **类型**：静态单页应用（`index.html` + JS/CSS 包）。
- **构建输出目录**：`skin2d-editor/dist/`（Vite 默认）。
- **源码根**：仓库根下 `skin2d-editor/`。

## 与构建工具对应

- Vite 项目名 `skin2d-editor`，`npm run build` 生成生产包。
