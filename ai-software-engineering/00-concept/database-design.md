# 数据库 / 存储设计

## 范围

本工具以浏览器端为主，**不强制**服务端数据库。工程数据以如下形态存在：

## 工程文件（后续）

- 建议单文件 JSON 或二进制包（ZIP）包含：元数据、骨骼、动画、资源引用路径。
- MVP 阶段：**内存状态 + 可选 LocalStorage 键**（仅存最近打开文件名等轻量信息）。

## 缓存

- 导入的纹理与解析结果可缓存在内存；大资源使用 `URL.createObjectURL` 生命周期由会话管理。

## 与概念层关系

持久化格式在落地后以 `02-physical/skin2d-editor-web/spec.md` 为准扩展。
